#include "main.h"
/**
 * _strcmp - will compare two strings
 * @s1: is the first string to compare and
 * @s2:is the second
 * Returns: 0 if they are similar
 * <0 and >0 if one is greater
 */
int _strcmp(char *s1, char *s2)
{
	return (0);
}
