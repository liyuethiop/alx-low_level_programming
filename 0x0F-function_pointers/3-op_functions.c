#include "3-calc.h"
/**
 * op_add - perform addition
 * @a:the first number
 * @b: the second number
 * Return: sum
 */
int op_add(int a, int b)
{
	return (a + b);
}
/**
 * op_add - perform subtraction
 * @a:the first number
 * @b: the second number
 * Return: difference
 */
int op_sub(int a, int b)
{
	return (a - b);
}
/**
 * op_add - perform multiplication
 * @a:the first number
 * @b: the second number
 * Return: product
 */
int op_mul(int a, int b)
{
	return (a * b);
}
/**
 * op_add - perform division
 * @a:the first number
 * @b: the second number
 * Return: a/b
 */
int op_div(int a, int b)
{
	return(a / b);
}
/**
 * op_add - perform modulo
 * @a:the first number
 * @b: the second number
 * Return: remainder
 */
int op_mod(int a, int b)
{
	return (a % b)
}
